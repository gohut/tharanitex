# Set these as server-side Cloudflare secrets for deployed environments.
# Never prefix the secret with NEXT_PUBLIC_ and never commit real values.
RAZORPAY_KEY_ID=rzp_test_your_public_key_id
RAZORPAY_KEY_SECRET=your_server_side_razorpay_secret
